import 'zone.js';
import { bootstrapApplication } from '@angular/platform-browser';
import { InstanceConsoleComponent } from './instance-console.component';

bootstrapApplication(InstanceConsoleComponent).catch(console.error);
